# abyss-builds

Dist desktop d'Abyss (fork rebrandé d'Equicord, GPL-3.0).
Fichiers consommés par l'injecteur du multitool : `patcher.js`, `preload.js`, `renderer.js`, `renderer.css`.
Build standalone, updater interne désactivé. Publié depuis le repo `0ctane6/abyss`.

Outil annexe : `tools/opencode-zen-mcp/` — serveur MCP qui branche OpenCode Zen (modele Ox Alpha) sur Claude Desktop.
